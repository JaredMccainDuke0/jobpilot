# JobPilot 项目交接摘要

> 当前架构说明（2026-09-05）：本文件较早章节中的“点击时调用 Terra `web_search`”属于历史实现记录，已由定时岗位库架构替代。当前用户匹配只读取本地新鲜岗位库；请以 `docs/JOB_CATALOG.md`、`docs/ARCHITECTURE.md` 和实际代码为准。

## 当前架构补充（2026-09-05）

- 岗位由固定后台程序按默认每 60 分钟从管理员配置的公开 Greenhouse、Lever、JSON、RSS 或 Atom 源采集。
- 采集程序负责确定性解析、字段校验、去重、发布时间/过期时间处理、更新时间窗口、容量限制和历史清理。
- `/api/matches` 不再调用模型或进行用户触发的联网搜索，只读取 `catalog_feed` 中的新鲜岗位并执行本地匹配评分。
- `config/job-feeds.json` 是运行时岗位源配置，已被 Git 忽略；仓库提供 `config/job-feeds.example.json` 和 `docs/JOB_CATALOG.md`。
- 可通过 `npm run catalog:worker` 长期运行独立 worker，或通过 `npm run catalog:refresh` 执行一次更新。Next.js Node 进程也会自动启动调度器。
- 显式过期岗位立即隐藏；默认 48 小时未再次出现的岗位不再匹配；有效岗位默认上限 5000；不被用户历史引用的过期岗位默认 90 天后清理。
- 当前未配置真实岗位源时，岗位库会如实显示为空，不使用示例岗位或模型搜索补足。

以下内容保留为历史实现记录，不作为当前架构和运行方式的依据。

更新时间：2026-08-12  
工作区：`C:\Users\35491\OneDrive\桌面\Work\Project\JobPilot`

## 1. 项目目标

JobPilot 是一个手机浏览器优先的求职网页应用。核心流程为：

1. 用户上传并确认简历。
2. 用户填写自然语言求职需求、城市、工作方式等条件。
3. 系统通过 Terra Responses API 的 `web_search` 搜索真实岗位。
4. 每批最多显示 5 个结果，并排除该用户此前看过的来源 URL。
5. 只接受官方一手页面明确公开招聘邮箱的岗位，不推断或伪造邮箱。
6. 用户勾选岗位并完成最终确认后，系统使用用户配置的 SMTP 发送简历。

岗位名称不要求与用户输入完全一致。搜索范围应覆盖与简历或意向能力接近的岗位，包括大模型、生成式 AI、智能体、RAG、NLP、机器学习、深度学习、算法、Python AI、AI 后端/平台、通信与 AI 等相邻方向。

## 2. 不可更改的约束

- 指定城市是硬条件。
- 每批最多返回 5 个岗位。
- 正式结果不得使用虚拟职位、示例职位或 `example.com` 补足。
- 岗位必须来自官方公司或研究机构一手页面。
- 页面必须明确公开招聘邮箱；邮箱不能由模型猜测、推断或生成。
- 排除大学教学、实验室、科研助理和校内研究岗位。
- 刷新搜索时排除该用户已经展示过的实时来源 URL。
- 搜索本身不得发送邮件。
- 只有用户勾选岗位并在确认页执行最终投递，才能发送真实邮件。
- 自动化测试不得发送真实邮件。
- API Key、SMTP 密码、登录凭据、个人简历内容不得输出、提交或写入报告。
- 不绕过登录、验证码、反爬、访问限制或网站规则。

## 3. 技术栈和架构

- Next.js 15、React 19、TypeScript。
- SQLite，通过 Node.js `node:sqlite` 使用。
- Zod 用于外部搜索结果校验。
- Vitest 用于单元测试。
- Nodemailer 用于 SMTP 投递。
- PDF.js 和 Mammoth 用于简历解析。
- 本地单用户起步，现已包含多用户数据隔离基础。

主要边界：

- `src/components/JobPilot.tsx`：主要手机端界面和流程。
- `src/app/api/`：简历、偏好、匹配、选择、投递、设置、SMTP 和认证接口。
- `src/infrastructure/job-search.ts`：Terra `web_search` 搜索适配器。
- `src/domain/matching.ts`：城市、工作方式等确定性规则和相近能力评分。
- `src/domain/application.ts`：投递资格判断。
- `src/infrastructure/submission.ts`：模拟和真实邮件适配器。
- `src/infrastructure/db.ts`：实际 SQLite 建表与增量字段迁移。
- `src/infrastructure/auth.ts`：账户、会话和用户秘密加密。

注意：`prisma/schema.prisma` 不是当前数据库结构的完整事实来源。实际运行结构由 `src/infrastructure/db.ts` 创建和迁移，已经包含 `users`、`user_settings`、`user_secrets`、各业务表的 `userId`、`jobs.applicationEmail` 和 `match_runs.searchWarning` 等字段。

## 4. 实时职位搜索当前实现

`src/infrastructure/job-search.ts` 当前行为：

- 从服务器环境读取模型 Base URL、模型名和 API Key。
- 请求 `${BASE_URL}/responses`。
- 请求体包含 `tools: [{ type: "web_search" }]`。
- 搜索提示包含当前简历结构化事实、求职需求、城市和已展示 URL。
- 岗位名称允许与意向名称不同，只需属于相近能力方向。
- 要求官方页面同时证明岗位和公开招聘邮箱。
- 只接受带模型网页引用的结果。
- 使用 Zod 校验职位、公司、城市、描述、邮箱和 URL。
- 过滤未引用 URL、错误城市、`example.com` 和历史 URL。
- 使用来源 URL、邮箱和岗位名生成稳定 ID 并去重。
- 最终截取最多 5 个结果。
- 搜索失败、超时或结果不足时返回明确警告，不回退演示职位。

`src/app/api/matches/route.ts` 当前行为：

- 读取当前用户已确认简历和求职需求。
- 查询该用户以前展示过的 `model_web_search` 来源 URL。
- 调用实时搜索适配器。
- 运行确定性匹配和相近能力评分。
- 将实时来源、岗位、匹配批次及警告写入 SQLite。
- 返回最多 5 个结果。

## 5. 投递行为

- 实时合格岗位写入为 `applicationType: "verified_email"`。
- 用户不需要再次填写公司、岗位或收件邮箱。
- 用户需要先配置自己的 SMTP 发件设置。
- 用户在匹配页勾选岗位，进入确认页后执行投递。
- `/api/applications` 使用已确认的简历文件作为附件。
- `idempotencyKey` 和数据库唯一约束用于避免重复投递。
- 状态包括 `WAITING`、`PROCESSING`、`SUCCESS`、`FAILED`、`NEEDS_USER` 和 `CANCELLED`。
- 本轮以及最近的自动化验证均未发送真实邮件。

## 6. 本轮明确修改的文件

- `src/infrastructure/job-search.ts`
  - 修复损坏的中文字符串和语法。
  - 保留 Terra Responses API `web_search` 实现。
  - 增加严格引用、城市、官方邮箱、历史 URL 和五条上限校验。
  - 放宽岗位名称要求，搜索相近 AI/算法/通信与 AI 岗位族。
- `src/app/api/matches/route.ts`
  - 修复乱码和损坏语法。
  - 保持真实搜索、历史 URL 排除和零结果不回退逻辑。
- `src/domain/matching.ts`
  - 修复乱码。
  - 增加大模型、智能体、机器学习、NLP、算法、Python、通信和工程化等能力组匹配。
- `src/infrastructure/job-search.test.ts`
  - 新增搜索适配器测试。
- `src/domain/matching.test.ts`
  - 重建乱码测试并增加“岗位名不同但能力相近”的测试。
- `docs/TEST_REPORT.md`
  - 记录实时 `/api/matches` 验证结果和当前未验证项。

## 7. 已执行验证

最近一次确认结果：

- `npm test`：5 个测试文件、14 项测试全部通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：停止运行中的生产服务后通过。
- `http://localhost:3000/invite`：交接文档创建前返回 HTTP 200。
- 3000 端口由 JobPilot Next.js 生产服务监听。

搜索适配器测试覆盖：

- 请求确实携带 `web_search` 工具。
- 排除 URL 被发送给模型。
- 每批最多 5 条。
- 未被引用、错误城市、`example.com` 和历史结果被过滤。
- 模型没有执行网页搜索时返回空结果，不回退虚拟职位。
- 岗位名不同但能力相近时仍可被判定为可选。

真实 `/api/matches` 曾执行两批：

- 一批模型请求在 120 秒后超时。
- 一批模型完成搜索，但严格校验后为 0 个结果。
- 两批均未使用演示职位补足。
- 两批来源 URL 重叠为 0。
- 没有调用投递或 SMTP 接口。

## 8. 当前运行和 Git 状态

- 分支：`master`。
- Git 仓库没有任何提交。
- 项目文件均显示为未跟踪状态。
- `.env` 被 `.gitignore` 忽略。
- `.local-logs/` 当前未被忽略，Git 状态中可见；需要补入 `.gitignore`。
- 本地 SQLite、上传简历和运行数据不得加入 Git。
- 生产服务当前监听 3000 端口。
- 曾验证公网地址 `https://cartwheel-synopsis-handyman.ngrok-free.dev/` 返回 JobPilot 和 HTTP 200，但交接文档创建时的公网检查没有得到可解析结果。Claude Code 必须重新实测成功后才能继续提供该链接。

## 9. 已知问题和风险

1. 尚未得到一批非空的实时结果，因此“官方页面、公开邮箱、前端展示、勾选、真实 SMTP 投递”的完整链路尚未端到端验证。
2. 真实搜索偶发 120 秒超时，需要考虑重试、查询拆分或更好的超时提示。
3. 当前引用校验要求模型引用 URL 与返回 `sourceUrl` 规范化后完全相等。官方页面重定向、引用落到同域相关页面或 URL 路径差异可能导致真实岗位被过滤。
4. 当前城市校验使用简单字符串包含关系，对“深圳市”“深圳/广州”“工作地点详见正文”等格式不够稳健。
5. “官网页面同时证明岗位和邮箱”的要求很严格，但这是用户明确要求，不能通过猜测邮箱放宽。
6. 若官方招聘公告列出多个岗位和统一招聘邮箱，需要允许同一官方页面生成多个不同岗位，同时保持稳定去重。
7. `src/app/api/invite/route.ts`、`src/app/api/applications/route.ts`、`src/infrastructure/secrets.ts` 等文件仍存在历史乱码字符串，需要逐个修复并补测试。
8. 运行中执行 `npm run build` 可能因 SQLite 写锁失败。安全流程是停止 JobPilot 服务、构建、再启动；不能删除或重置数据库。
9. Node.js 22 会提示 `node:sqlite` 为实验功能。
10. 新一轮 Playwright 手机截图未成功产生文件，不能把旧截图当成当前版本的完整视觉验收。

## 10. Claude Code 建议续接顺序

1. 先执行只读检查：`git status`、关键文件内容、3000/4040/4041 端口、Node/ngrok/cloudflared 进程、非敏感配置项是否存在。
2. 不输出 `.env` 的值，不读取或展示简历正文，不展示数据库中的个人字段。
3. 确认 `src/infrastructure/job-search.ts`、`src/app/api/matches/route.ts` 和 `src/infrastructure/db.ts` 与本摘要一致。
4. 补充搜索适配器测试：同域引用跳转、招聘公告多岗位、城市规范化、过期日期、重复邮箱和重复来源。
5. 改善检索召回率，但不得降低“官方页面明确公开邮箱”的真实性要求。可以拆分相近岗位族为多条搜索查询后合并去重。
6. 使用现有确认用户执行真实 `/api/matches`，只输出数量、来源类型、警告和 URL 重叠等脱敏摘要。
7. 得到非空结果后，逐条服务器端验证官方页面是否确实包含岗位和邮箱；不把模型陈述本身当最终证明。
8. 不发送真实邮件。先验证选择、确认页、附件存在性和投递资格。
9. 运行 `npm test`、`npx tsc --noEmit`；生产构建前先停止服务，构建后重新启动。
10. 用 Playwright 检查 375x812、390x844、430x932 和 1280x800，确认无横向滚动、遮挡或固定栏重叠。
11. 更新 `docs/TEST_REPORT.md`，明确通过、失败和未验证项。
12. 重新启动隧道并实际从公网请求成功后再提供链接。

## 11. 常用命令

```powershell
cd "C:\Users\35491\OneDrive\桌面\Work\Project\JobPilot"
git status --short --branch
npm test
npx tsc --noEmit
npm run build
npm run start
```

检查本地服务：

```powershell
Invoke-WebRequest -UseBasicParsing http://localhost:3000/invite
Get-NetTCPConnection -State Listen -LocalPort 3000
```

启动隧道前先确认是否已有隧道进程或固定端点在线。此前 ngrok 新建同名端点曾返回：

```text
ERR_NGROK_334: endpoint is already online
```

## 12. 完成标准

- 用户能上传并确认简历、填写需求和城市。
- 系统搜索相近能力方向的真实岗位，而非要求职位名完全一致。
- 每批最多 5 个，刷新排除已展示 URL。
- 每个结果均由官方一手页面和网页引用支持，并明确公开招聘邮箱。
- 不足 5 个时展示真实数量，不使用虚拟数据补齐。
- 用户选择并最终确认后可通过自己的 SMTP 投递，无需再次填写岗位资料。
- 重复请求不会重复投递。
- 自动化测试不产生真实外部邮件副作用。
- 手机端核心流程可操作，生产构建和公网入口均经过实际验证。

## 13. 交接启动提示词

请先读取项目根目录的 `摘要文档.md`、`docs/TEST_REPORT.md` 和实际工作区文件。先执行只读检查，包括 Git 状态、关键实现、实际 SQLite 结构、端口、进程和非敏感配置状态。不要假设摘要中的修改一定仍在磁盘，不要重复已经完成且仍然存在的工作。保护 `.env`、API Key、SMTP 密码、账户凭据、数据库、上传简历和个人信息。重点继续提高 Terra `web_search` 对相近岗位的召回率，但必须坚持官方页面明确公开招聘邮箱、每批最多 5 条、刷新排除旧结果和不回退虚拟职位。测试中不得发送真实邮件。完成后运行单元测试、TypeScript、生产构建和 Playwright 手机端验收，更新 `docs/TEST_REPORT.md`，并仅在公网请求实际成功后提供链接。
