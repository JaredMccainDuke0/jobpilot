# JobPilot 项目交接文档

更新时间：2026-09-05（Asia/Shanghai）

## 当前版本

- 工作目录：`C:\Users\35491\OneDrive\桌面\Work\Project\JobPilot`
- 当前分支：`master`
- 远端仓库：`VELIR5/jobpilot`（公开仓库）
- 当前 HEAD：以 `git log -1 --oneline` 为准
- 工作区状态：本次岗位库架构更新已完成，提交并推送后应与 `origin/master` 同步
- 公网地址：`https://job.vcrelay.com:8443`

## 产品功能

网页端支持邮箱登录、简历上传与解析确认、求职偏好设置、真实岗位搜索、岗位匹配、岗位选择、岗位详情、投递前确认、投递记录和平台域名统一代发。

岗位库只保留真实、符合城市与方向、来源记录结构完整且处于新鲜窗口的岗位。显式过期、长期未更新、无有效来源或重复岗位不会进入候选结果。

平台代发使用用户登录邮箱作为 `Reply-To`，并密送一份副本给用户。真实邮件必须经过用户确认；测试不发送真实邮件。

## 技术结构

- H5：Next.js App Router、React、TypeScript
- 服务端：Next.js API Routes、Node.js 22 内置 SQLite
- 岗位库：固定程序按计划采集公开 Greenhouse、Lever、JSON、RSS 或 Atom 源
- 邮件：平台域名统一代发，服务端保存发信密钥
- 测试：Vitest
- 小程序：`miniapp/` 下的原生微信小程序前端

H5 与小程序共用服务端业务层和 API。网页端使用签名 httpOnly Cookie；小程序使用独立 Bearer 会话。服务端认证同时支持两种方式，但不会把小程序 Token 写入网页 Cookie。岗位采集、过期和去重只在服务端固定程序中执行。

## 小程序当前状态

小程序目录可直接导入微信开发者工具，已包含：

- `miniapp/project.config.json`
- 微信登录会话交换与邮箱绑定页面
- 首页
- 简历上传
- 求职偏好
- 岗位搜索和选择
- 岗位详情
- 投递确认
- 投递记录

小程序配置文件中的 API 地址指向当前公网 HTTPS 服务。正式运行前需要在微信开发者工具中替换自己的 AppID，并在微信平台配置合法 request/upload 域名。微信 AppID、AppSecret、邮件 Key 和数据库均不在仓库中。

## 重要接口

- `POST /api/invite`：网页邮箱登录
- `POST /api/miniapp/session`：小程序 `wx.login` code 换会话；`PUT` 用已有邮箱绑定
- `GET /api/state`：读取当前用户状态、匹配结果和投递任务
- `POST /api/resume`：上传简历
- `POST /api/resume/confirm`：确认简历解析
- `POST /api/preferences`：保存求职偏好
- `POST /api/matches`：读取岗位库并执行本地匹配
- `POST /api/matches/select`：选择或取消选择岗位
- `POST /api/applications`：创建已选择岗位的投递任务

## 本地运行

```powershell
npm ci
npm run db:push
npm run dev
```

生产验证：

```powershell
npm test
npx tsc --noEmit
npm run build
npm run start
```

本地数据库默认位于 `%LOCALAPPDATA%\JobPilot\jobpilot.db`。`.env`、数据库、上传文件和日志均被忽略，不得提交到 GitHub。

## 配置边界

实际运行需要在部署环境提供会话、访问门槛、数据库、岗位源和平台代发配置。只允许提交 `.env.example` 中的变量名和占位符；不得在代码、README、日志、测试输出或小程序包中写入任何 Key、Secret、访问密码、简历内容或个人字段。

当前岗位源配置不随 Git 提交。线上服务器是否已配置公开岗位源、调度器是否运行，需要在服务器环境中单独核对。

## 已验证结果

- 20 个测试文件、59 项测试通过
- TypeScript 检查通过
- Next.js 生产构建通过
- 小程序 JavaScript 语法检查通过
- 小程序 JSON 配置解析通过
- 当前公网首页最近一次核对返回 HTTP 200
- 岗位源适配器、刷新锁、过期清理和本地岗位库查询已加入自动化测试
- 未发送真实邮件

## 未完成或需要人工验证

1. 使用真实微信开发者工具和真机验证微信登录、邮箱绑定、文件上传、页面跳转和 Bearer 访问。
2. 在微信平台配置正式 AppID、合法 request/upload 域名和隐私说明。
3. 在服务器部署环境配置经授权的公开岗位源，并确认调度器或 `npm run catalog:worker` 正在运行。
4. 在服务器部署环境确认最新 `master` 已拉取，并重新构建和重启服务。
5. 完成小程序投递流程的真机回归；自动化测试不会代替微信环境验证。

## 安全与操作禁区

- 不输出或提交 API Key、OAuth Secret、邮件服务 Key、访问密码、会话密钥。
- 不输出真实简历、姓名、电话、邮箱或数据库个人字段。
- 不重置、删除或迁移现有数据库。
- 不擅自发送真实邮件；真实投递必须由用户在目标岗位级别确认。
- 不猜测招聘邮箱，不虚构岗位、来源、发送结果或送达证据。
- 不绕过招聘网站登录、验证码、反爬或访问限制。
- 修改服务端后必须重新构建并重启 `next start`，运行中的进程不会自动加载新构建。

## 继续工作建议

优先配置经授权的公开岗位源并运行 `npm run catalog:refresh` 验证岗位库，再使用微信开发者工具导入 `miniapp/` 做真机联调。若发现接口问题，先在服务端补充测试，再同步修复小程序调用。任何涉及网页端认证的改动，都必须同时回归邮箱登录和 Cookie 会话。
